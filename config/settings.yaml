# Application Configuration
system:
  app_name: "Smart Task Manager Pro"
  version: "1.0.0"
  environment: "production"

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

defaults:
  priority: "MEDIUM"
  max_tasks_per_view: 50